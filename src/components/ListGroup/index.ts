// The index.ts file is a simplified way to help with other import methods 
// find this file as a combined source for all of the ListGroup files 
// instead of just calling a specific file. The compiler by default will
// look for the index.ts file if the specified file is not mentioned.
import ListGroup from "./ListGroup";

// Export as a default object.
export default ListGroup;